    <div class="timerpro onspro next-prevb">
      <div class="head-t"><h4 class="heading"><svg width="15px" height="15px"><use xlink:href="#star"></use></svg><span>{{ heading_title }}</span></h4><hr></div>
<div class="row rless"> 
  <div id="count" class="owl-theme owl-carousel fnb">
  {% for product in products %}
  <div class="product-layout col-xs-12 cless">
    <div class="product-thumb transition">
      <div class="image"><a href="{{ product.href }}"><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-responsive" /></a>
         <!-- Webiarch Images Start -->
                        {% set t = 0 %}
                          {% for webi_image in product.webi_images %}
                          {% if t == 0 %}
                           <a href="{{ product.href }}"><img src="{{ webi_image.popup }}" class="img-responsive second-img" alt="hover image"/></a>
                          {% set t = t + 1 %}
                          {% endif %}
                        {% endfor %}
        <!-- Webiarch Images End -->
          {% if product.tag_per  %}
        <span class="sale">{{ product.tag_per }}%</span>
        {% endif %}
        {% if product.price %}
          {% if product.special %}
               <h3 class="sale">sale</h3>
          {% endif %}
      {% endif %}   

      </div>
      <div class="caption text-center">
         
         {% if product.rating %}
          <div class="rating">
            {% for i in 1..5 %}
            {% if product.rating < i %}
            <span class="fa fa-stack">
              <i class="fa fa-star-o fa-stack-2x"></i>
            </span>
            {% else %}
            <span class="fa fa-stack">
              <i class="fa fa-star fa-stack-2x"></i><i class="fa fa-star-o fa-stack-2x"></i>
            </span>
            {% endif %}
          {% endfor %}</div>{% else %}
          <div class="rating">{% for i in 1..5 %}
          <span class="fa fa-stack"><i class="fa fa-star-o fa-stack-2x"></i></span>
          {% endfor %}</div>
        {% endif %}

         <h4 class="protitle"><a href="{{ product.href }}">{{ product.name }}</a></h4>
      
         {% if product.price %}
        <div class="price">
          {% if not product.special %}
          {{ product.price }}
          {% else %}
          <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span>
          {% endif %}
         {#  {% if product.tax %}
          <span class="price-tax">{{ text_tax }} {{ product.tax }}</span>
          {% endif %} #}
        </div>
        {% endif %}
         {% if config_module.countdown %}<div id="Countdown{{ product.product_id }}" class="box-timer"></div> {% endif %}
        {# <p>{{ product.description }}</p> #}
           <!--  <div class="button-group">

      <div class="absbtn">
        <div class="addtocart"><button class="btn-block" type="button" onclick="cart.add('{{ product.product_id }}');"><svg width="19px" height="19px"><use xlink:href="#pcart"></use></svg></button></div>
        <div class="wish"><button type="button" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}');" class="bwish"><svg width="19px" height="19px"><use xlink:href="#pwish"></use></svg>{# {{ button_wishlist }} #}</button></div>
        <div class="view"><button type="button" title="{{ button_compare }}" onclick="compare.add('{{ product.product_id }}');" class="bcom"><svg width="16px" height="16px"><use xlink:href="#pcom"></use></svg>{# {{ button_compare }} #}</button></div>
       <div class="bquickv" title="Quickview"> </div> 
      </div>
    </div> -->

      </div>
    

   {% if product.date_end %}
            <script type="text/javascript">
                $(document).ready(function () {
                    $('#Countdown{{ product.product_id }}').countdown({
                       until: new Date({{ product.date_end|date("Y") }}, {{ product.date_end|date("m") }}-1, {{ product.date_end|date("d") }}, {{ product.date_end|date("H") }}, {{ product.date_end|date("i") }}, {{ product.date_end|date("s") }}),
                        labels: ['Years', 'Months', 'Weeks', 'Days', 'Hrs', 'Mins', 'Secs'],
                        labels1: ['Year', 'Month', 'Week', 'Day', 'Hr', 'Min', 'Sec'],
                    });
                    //$('#Countdown<?php echo $product['product_id'];?>').countdown('pause');
                });
            </script>
            {% endif %}
            </div>
    </div>
  {% endfor %}


  
   </div>
 </div>
</div>

<script type="text/javascript">
    $(document).ready(function() {
    $("#count").owlCarousel({
    itemsCustom : [
    [0, 1],
    [360, 1],
    [450, 2],
    [650, 3],
    [768, 1],
    [992, 1],
    [1200, 1],
    [1590, 1]
    ],
      // autoPlay: 1000,
      navigationText: ['<i class="fa fa-angle-left" aria-hidden="true"></i>', '<i class="fa fa-angle-right" aria-hidden="true"></i>'],
      navigation : true,
      pagination:false
    });
    });
</script>
